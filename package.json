{
  "name": "empactme-site",
  "version": "1.0.0",
  "description": "EmpactMe marketing site with live roadmap",
  "scripts": {
    "build": "npx @11ty/eleventy",
    "serve": "npx @11ty/eleventy --serve"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0"
  }
}
